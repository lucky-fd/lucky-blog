server:
  port: 8604

spring:
  application:
    name: mogu-sms
  cloud:
    nacos:
      username: nacos
      password: nacos

      discovery:
        server-addr: localhost:8858
        namespace: dev
      config:
        server-addr: localhost:8858
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
